<h2> <%=@mountain.name%> </h2>

<%= image_tag @mountain.image, :class => 'feature' if @mountain.image.present? %>

<p><strong>Elevation:</strong> <%= @mountain.elevation %> meters.</p> <br>
<p><strong>Country:</strong> <%= @mountain.country %>.</p> <br>
<p><strong>First Ascent: </strong><%= @mountain.first_ascent %> </p>

<div class="controller">
  <%= link_to 'Edit', edit_mountain_path(@mountain.id)  %>
  <%= button_to "Delete mountain", mountain_path(@mountain.id), :method => 'delete' %>
</div>
